#include <stdio.h>
#include <stdlib.h>

int main()
{
   int i;
   printf("nhap so: ");
   scanf("%d",&i);
   if(i % 2 == 0){
    printf("Day la so chan.");
   }else{
   printf("Day la so le.");}

    return 0;
}
